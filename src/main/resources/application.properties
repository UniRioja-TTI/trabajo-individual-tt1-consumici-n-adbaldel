spring.application.name=trabajo

# Puerto del servidor Spring Boot (la VM ocupa el 8080 del host via port-forwarding)
server.port=9090

# URL base del servidor de simulaciones (VM: puerto 5000 interno -> 8080 del host)
consumible.base-url=http://localhost:8080
